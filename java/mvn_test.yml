---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: kkmbal/maven-build

params:
  SKIP_TEST: no
  GOAL: test

inputs:
- name: git
- name: ci

outputs:
- name: deploy

run:
  path: ci/java/mvn.sh
