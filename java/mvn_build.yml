---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: kkmbal/maven-build

params:
  SKIP_TEST: yes
  GOAL: build

inputs:
- name: git
- name: ci

outputs:
- name: deploy

run:
  path: ci/java/mvn.sh
