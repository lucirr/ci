---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: kkmbal/maven-build

params:
  FILE:

inputs:
- name: git
- name: manifest

outputs:
- name: deploy

run:
  path: manifest/java/mvn_package.sh
