---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: kkmbal/maven-build

inputs:
- name: git, manifest

outputs:
- name: deploy

run:
  path: manifest/ci/mvn_package.sh
